name: run unit tetsts of feature branches

on:
  push:
    branches: 
     'feature/*'
     
    jobs:
       run-unit-tests:
          runs on: ubuntu-latest
          
          steps: 
           - name: Checkout
           uses: actions/checkout@v3.5.3
           
           - run: unit test
           run: mvn test 
